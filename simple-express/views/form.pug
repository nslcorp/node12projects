extends layout

block content
    .container
        form(method='post', action='contact/send')
            h2 Contact of Person
            .form-group
                lable name
                input.form-control(type='text' name='name', placeholder='Enter name...')
